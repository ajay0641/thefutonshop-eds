{
  "definitions": [{
    "title": "Product List Page",
    "id": "product-list-page",
    "model": "product-list-page",
    "plugins": {
      "da": {
        "type": "key-value-block"
      }
    }
  }],
  "models": [{
    "id": "product-list-page",
    "fields": [
      {
        "component": "text",
        "name": "urlpath",
        "label": "URL Path",
        "description": "Catalog category urlPath (e.g. apparel). Required for DA preview and enrichment. Live category URLs override this value."
      },
      {
        "component": "text",
        "name": "defaultCateId",
        "label": "Default Category ID",
        "description": "Optional: category ID for template preview in DA/UE when urlpath is not set (e.g. 6 for a category)"
      },
      {
        "component": "number",
        "name": "pageSize",
        "label": "Page Size",
        "value": 9,
        "description": "Number of products per page (default: 9)"
      }
    ]
  }],
  "filters": []
}
